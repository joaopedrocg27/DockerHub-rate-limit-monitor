global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: docker_rate_limit
    static_configs:
      - targets: ["exporter:8000"]
    scrape_interval: 60s   # match the exporter poll interval
    scrape_timeout: 20s
