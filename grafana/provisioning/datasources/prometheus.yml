apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: docker_rl_prom
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "60s"
